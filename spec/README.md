# Spec Workspace

This directory is reserved for language-neutral artifacts that support the canonical documents in `docs/`.

Planned contents:
- machine-readable schemas for conformance vectors
- release checklists
- compatibility notes when the public spec changes
