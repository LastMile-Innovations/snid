[package]
name = "snid-py"
version = "0.1.0"
edition = "2021"

[lib]
name = "snid_native"
crate-type = ["cdylib"]

[features]
default = ["data"]
data = []

[dependencies]
pyo3 = { version = "0.28.3", features = ["extension-module"] }
snid = { path = "../rust", features = ["data"] }
